# Общие TLS-настройки публичных vhost'ов. Пути сертификата — в шаблонах
# vhost'ов (${CERT_NAME}); сертификат один, SAN на все домены копии.
ssl_protocols       TLSv1.2 TLSv1.3;
ssl_session_cache   shared:SSL:10m;
ssl_session_timeout 1h;
